import React from 'react';

const Solution = () => {
    return (
        <section className="section solution" id="about">
            <div className="container">
                <span className="eyebrow">The Evolution</span>
                <h2>Elegant workflow for professional scripts.</h2>
                <p>
                    VidSift isn't just a transcriber. It's a semantic bridge between spoken word and the written page, designed for professionals who demand precision, speed, and privacy.
                </p>
            </div>
        </section>
    );
};

export default Solution;
